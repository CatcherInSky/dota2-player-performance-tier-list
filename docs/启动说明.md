# 🚀 启动说明

## 快速开始（3步）

### 第一步：安装依赖

\`\`\`bash
npm install
\`\`\`

**等待安装完成**，这会安装所有必需的依赖包。

---

### 第二步：选择开发方式

#### 方式 A：浏览器开发（推荐用于 UI 开发）

\`\`\`bash
npm run dev
\`\`\`

打开 http://localhost:5173 在浏览器中预览。

**注意**: 某些 Overwolf API 在浏览器中不可用。

#### 方式 B：Overwolf 环境（推荐用于功能测试）

1. 构建应用：
\`\`\`bash
npm run build
\`\`\`

2. 在 Overwolf 中加载：
   - 打开 Overwolf
   - 右键点击托盘图标 → Settings
   - Support → Development options
   - "Load unpacked extension"
   - 选择 `dist/` 文件夹

---

### 第三步：测试功能

#### ✅ 桌面窗口（窗口1）
- 应用启动后会自动打开
- 显示应用状态和调试控制

#### ✅ 游戏内窗口（窗口2）

**在浏览器/桌面窗口中测试：**
1. 点击"策略阶段"按钮 → 触发策略时间事件
2. 点击"赛后阶段"按钮 → 触发赛后事件

**在 Overwolf 中测试（需要 Dota 2）：**
1. 启动 Dota 2
2. 进入游戏选人阶段 → 游戏内窗口自动弹出（显示 2）
3. 游戏结束 → 游戏内窗口显示赛后内容（显示 3）

#### ✅ 热键功能
- 按 **Alt + Shift + D** → 隐藏/显示所有窗口
- 只在 Overwolf 环境中有效

---

## 🏗️ 项目架构

### Overwolf Native App 架构

\`\`\`
┌─────────────────────────────────────┐
│   Overwolf Platform (平台层)        │
├─────────────────────────────────────┤
│   Background Controller (后台控制器) │
│   - 管理窗口生命周期                 │
│   - 监听游戏事件                     │
│   - 处理热键                         │
│   - 协调窗口通信                     │
├─────────────────────────────────────┤
│  ┌───────────┐    ┌───────────┐     │
│  │ 桌面窗口  │    │游戏内窗口 │     │
│  │ (Desktop) │    │ (Ingame)  │     │
│  │  React    │    │  React    │     │
│  └───────────┘    └───────────┘     │
└─────────────────────────────────────┘
         ↓
    Dota 2 Game Events
\`\`\`

### 与 Electron 方案的区别

| 特性 | Overwolf Native | Electron (旧方案) |
|------|----------------|------------------|
| 运行时 | Overwolf 平台 | Electron + Node.js |
| 打包大小 | ~5-10 MB | ~100-150 MB |
| 游戏集成 | 原生支持 | 需要额外配置 |
| 性能 | 优秀 | 良好 |
| 复杂度 | 简单 | 复杂 |

---

## 📁 关键文件说明

### 配置文件

| 文件 | 说明 |
|------|------|
| `manifest.json` | **Overwolf 应用配置**（最重要） |
| `package.json` | NPM 依赖和脚本 |
| `vite.config.ts` | Vite 构建配置（多页面） |
| `tsconfig.json` | TypeScript 配置 |

### 源代码

| 文件 | 说明 |
|------|------|
| `src/background/background.ts` | **后台控制器**（管理应用） |
| `src/App.tsx` | 桌面窗口（主窗口） |
| `src/ingame.tsx` | 游戏内窗口 |
| `src/utils/overwolf.ts` | Overwolf API 封装 |

### HTML 入口

| 文件 | 说明 |
|------|------|
| `background.html` | Background controller 入口 |
| `index.html` | 桌面窗口入口 |
| `ingame.html` | 游戏内窗口入口 |

---

## 🔧 开发命令

\`\`\`bash
# 开发模式（浏览器）
npm run dev

# 构建生产版本
npm run build

# 代码检查
npm run lint

# 预览构建结果
npm run preview
\`\`\`

---

## 🎮 Overwolf 开发技巧

### 1. 调试 Background Controller

\`\`\`
Overwolf → 右键托盘图标 → Settings → Support
→ Development options → Developer console
→ 选择你的应用 → 查看 background 窗口日志
\`\`\`

### 2. 调试窗口

- 右键窗口 → "Inspect"
- 或在代码中添加 `debugger;`

### 3. 查看应用日志

在 Developer Console 中可以看到：
- Background 启动日志
- 游戏事件
- 窗口生命周期
- API 调用结果

### 4. 热重载

修改代码后：
1. 运行 `npm run build`
2. 在 Overwolf Developer Console → 点击 "Reload"

### 5. 模拟游戏事件

在桌面窗口中使用调试按钮：
- "策略阶段" → 模拟选人阶段
- "赛后阶段" → 模拟游戏结束

---

## ⚙️ 自定义配置

### 修改热键

编辑 `manifest.json`:

\`\`\`json
"hotkeys": {
  "toggle_windows": {
    "title": "Toggle All Windows",
    "action-type": "custom",
    "default": "Alt+Shift+D"  // ← 修改这里
  }
}
\`\`\`

### 修改窗口大小

编辑 `manifest.json`:

\`\`\`json
"desktop": {
  "size": {
    "width": 1200,  // ← 修改宽度
    "height": 800   // ← 修改高度
  }
}
\`\`\`

### 修改游戏事件监听

编辑 `src/background/background.ts` 的 `handleGameStateChange()` 函数。

---

## 🐛 常见问题

### 问题 1：在 Overwolf 中看不到应用

**解决**：
1. 确保已运行 `npm run build`
2. 检查 `dist/` 文件夹是否有文件
3. 确保加载的是 `dist/` 文件夹
4. 查看 Overwolf Developer Console 的错误信息

---

### 问题 2：游戏事件没有触发

**解决**：
1. 确保 Dota 2 正在运行
2. 检查 manifest.json 中 `game_ids` 是否包含 `7314`
3. 查看 background 日志确认监听器已启动
4. 确保在游戏中（不是菜单）

---

### 问题 3：热键不工作

**解决**：
1. 只在 Overwolf 环境中有效（浏览器中无效）
2. 检查是否与其他应用冲突
3. 在 Overwolf Settings → Hotkeys 中查看和修改

---

### 问题 4：窗口显示异常

**解决**：
1. 检查 manifest.json 窗口配置
2. 确保 `transparent` 设置正确
3. 检查 CSS 样式
4. 在 Developer Console 查看错误

---

### 问题 5：修改代码后没有生效

**解决**：
1. 运行 `npm run build` 重新构建
2. 在 Overwolf Developer Console 点击 "Reload"
3. 如果还不行，完全关闭应用再重新加载

---

## 📝 开发工作流

### 典型的开发流程

\`\`\`bash
# 1. 开发 UI（快速迭代）
npm run dev
# 在浏览器中开发界面和样式

# 2. 测试 Overwolf 功能
npm run build
# 在 Overwolf 中加载并测试

# 3. 修改代码
# 编辑文件...

# 4. 重新构建和测试
npm run build
# 在 Overwolf Developer Console 点击 Reload

# 5. 重复 3-4 直到完成
\`\`\`

---

## 🎯 下一步

1. ✅ 熟悉项目结构
2. ✅ 在浏览器中开发 UI
3. ✅ 在 Overwolf 中测试功能
4. ⏳ 启动 Dota 2 测试真实游戏事件
5. ⏳ 实现具体的业务功能

---

## 📚 参考资源

- [Overwolf 开发文档](https://overwolf.github.io/docs/start/getting-started)
- [Dota 2 游戏事件](https://overwolf.github.io/docs/api/live-game-data/supported-games/dota-2)
- [Overwolf API 参考](https://overwolf.github.io/docs/api/overwolf-api-overview)
- [官方示例应用](https://github.com/overwolf/front-app)

---

**准备好了吗？开始你的开发之旅吧！** 🚀

有问题查看 [README.md](./README.md) 或 [项目概览.md](./项目概览.md)
